@model IEnumerable<CustomerManagement.Models.BankData>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>Iban</th>
            <th>Bank Name</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Iban)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BankName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BankDataId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BankDataId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BankDataId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
